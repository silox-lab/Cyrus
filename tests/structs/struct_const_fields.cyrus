// @stdout: Cyrus (1999)

import std::libc{printf};

struct Book {
    pub title: uint8*,
    pub year: int32
}

pub fn main() {
    const book = Book { title: "Cyrus", year: 1999 };
    const title = book.title;
    const year = book.year;
    printf("%s (%d)\n", title, year);
}
