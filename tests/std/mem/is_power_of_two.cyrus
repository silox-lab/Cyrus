// @stdout: ok

import std::mem{is_power_of_two};
import std::libc{printf};

pub fn main() {
    @assert(is_power_of_two(2));
    @assert(is_power_of_two(4));
    @assert(is_power_of_two(8));
    @assert(is_power_of_two(32));
    printf("ok\n");
}