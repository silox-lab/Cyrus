// @stdout: 3 2

import std::libc{printf};

fn div_mod(x: int32, y: int32) (int32, int32) {
    return (x / y, x % y);
}

pub fn main() {
    const (q, r) = div_mod(17, 5);
    printf("%d %d\n", q, r);
}
