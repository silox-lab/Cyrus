// @stdout: 25

import std::libc{printf};

type IntOp = fn(int, int) int;

fn apply(x: int, y: int, op: IntOp) int {
    return op(x, y);
}

fn mul(a: int, b: int) int {
    return a * b;
}

pub fn main() {
    const result = apply(5, 5, mul);
    printf("%d\n", result);
}
