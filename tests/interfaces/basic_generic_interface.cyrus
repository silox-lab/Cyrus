// @stdout: yes

import std::libc{printf};

interface IValidator<T> {
    fn validate(&const self, value: T) bool;
}

struct NumberValidator: IValidator<int32> {
	pub fn validate(&const self, value: int32) bool {
		if (value > 5) {
			return true;
		} else {
			return false;
		}
	}
}

pub fn main() {
	const validator: IValidator<int32> = dynamic NumberValidator{};

	if (validator.validate(10)) {
		printf("yes\n");
	} else {
		printf("no\n");
	}
}
