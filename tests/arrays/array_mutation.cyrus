// @stdout: 10 20 30

import std::libc{printf};

pub fn main() {
    var arr = int32[3]{1, 2, 3};
    arr[0] = 10;
    arr[1] = 20;
    arr[2] = 30;

    for (var i: usize = 0; i < 3; i++) {
        printf("%d ", arr[i]);
    }
}
