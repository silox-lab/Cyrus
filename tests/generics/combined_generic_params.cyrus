// @stdout: 14 5

import std::libc{printf};

struct Point<T> {
    pub x: T,
    pub y: T,

    pub fn add_x<I>(&const self, x: I) I {
        return self->x + x;
    }
}

pub fn main() {
    const inst = Point { x: 10, y: 5 };

    printf("%d %d\n", inst.add_x(4), inst.y);
}