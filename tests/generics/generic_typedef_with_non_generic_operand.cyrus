// @stdout: 10 20

import std::libc{printf};

type Handler<T> = fn(T) void;
type Int<T> = T;

pub fn main() {
    const handler: Handler<uint32> = fn(x: uint32) void {
        printf("%d ", x);
    };

    const integer: Int<uint64> = 20;

    handler(10);
    printf("%d\n", integer);
}