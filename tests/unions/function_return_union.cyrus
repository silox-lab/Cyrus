// @stdout: Cyrus!

import std::libc{printf};

union Value {
    int_value: int64,
    text_value: char*
}

fn new_text_value(text_value: char*) Value {
    return Value { text_value };
}

pub fn main() {
    const inst = new_text_value("Cyrus!");

    printf("%s\n", inst.text_value);
}