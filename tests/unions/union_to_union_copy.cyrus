// @stdout: 10

import std::libc{printf};

union PaddedUnion {
    a: int32,
    b: int64,
    c: int8[12]
}

pub fn main() {
    const inst = PaddedUnion { b: 10 };
    var copy: PaddedUnion;

    copy = inst; // triggers memcpy 
    printf("%d\n", copy.b);
}