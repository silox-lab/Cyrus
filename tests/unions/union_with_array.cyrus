// @stdout: 1 2 3

import std::libc{printf};

union Payload {
    bytes: char*,
    data: int[3]
}

pub fn main() {
    var p = Payload { data: int[3]{1, 2, 3} };
    printf("%d %d %d\n", p.data[0], p.data[1], p.data[2]);
}
